
  <%# <h1>Teachers</h1>

  Our blog has <%= Teacher.count teachers and counting!

  <ul>
    <% @teachers.each do |teacher| %>
      
        <%# <li>
          <%= link_to teacher.name, teacher %>
        <%# </li>
     
    <% end %>
  <%# </ul> 

  <%= link_to "New Teacher", new_teacher_path %>  
  <h1>Listing Teachers and Students</h1>

<table class="table table-striped table-hover bg-light">
  <thead>
    <tr>
      <th>Name</th>
      <th>Subject</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @teachers.each do |teacher| %>
      <tr>
        <td><%= teacher.name %></td>
        <td><%= teacher.subject %></td>
        <td><%= link_to "Show", teacher, :class => "btn btn-primary" %></td>
        <td><%= link_to "Edit", edit_teacher_path(teacher), :class => "btn btn-warning" %></td>
        <td><%= link_to "Destroy", teacher_path(teacher), :class => "btn btn-danger", data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %></td>
        
        <%# <td><%= link_to "Destroy", teacher_path(@teacher), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure?"
                  } %>
      </tr>
    <% end %>
  </tbody>
</table>

<br>



